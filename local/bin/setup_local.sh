#!/usr/bin/env bash
start=$(pwd)
source /usr/local/etc/bashrc
## This script is intended for installing/setting up tools or
## libraries that are project-specific.  AFAIK the wellcome trust
## biopsies dataset has no such requirements.  If that turns out to
## not be true, then I will put them here.

cd $start
